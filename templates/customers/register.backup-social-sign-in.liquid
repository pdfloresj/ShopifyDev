<section
  class="customer login"
  data-section-id="{{ section.id }}"
  data-section-type="register"
>
  <div class="login__main">
    <h1 class="login__heading ff-heading fs-page-heading c-heading">{{ 'customer.register.title' | t }}</h1>

    {%- assign formId = 'RegisterForm' -%}
    {% form 'create_customer', id: formId, novalidate: 'novalidate' %}
      {% render 'form-status', form: form, form_id: formId %}

      <input
        type="text"
        name="customer[first_name]"
        placeholder="{{ 'customer.register.first_name' | t }}"
        id="{{ formId }}-FirstName"
        class="input input--round"
        {% if form.first_name %}value="{{ form.first_name }}"{% endif %}
        autocomplete="given-name"
      >

      <input
        type="text"
        name="customer[last_name]"
        placeholder="{{ 'customer.register.last_name' | t }}"
        id="{{ formId }}-LastName"
        class="input input--round"
        {% if form.last_name %}value="{{ form.last_name }}"{% endif %}
        autocomplete="family-name"
      >

      <input
        type="email"
        name="customer[email]"
        placeholder="{{ 'customer.register.email' | t }}"
        id="{{ formId }}-email"
        class="input input--round {% if form.errors contains 'email' %} input--error{% endif %}"
        {% if form.email %} value="{{ form.email }}"{% endif %}
        autocorrect="off"
        autocapitalize="off"
        autocomplete="email"
        aria-required="true"
        {%- if form.errors contains 'email' -%}
          class="input--error"
          aria-invalid="true"
          aria-describedby="{{ formId }}-email-error"
        {%- endif -%}
      >
    
     <input
        type="text"
        name="customer[tags][Birthday]"
        placeholder="Birthdate"
        id="{{ formId }}-bdate"
        class="input input--round {% if form.errors contains 'bdate' %} input--error{% endif %}"
        aria-required="true"
        {%- if form.errors contains 'bdate' -%}
          class="input--error"
          aria-invalid="true"
          aria-describedby="{{ formId }}-bdate-error"
        {%- endif -%}
      >

      <input
        type="password"
        name="customer[password]"
        placeholder="{{ 'customer.register.password' | t }}"
        id="{{ formId }}-password"
        class="input input--round {% if form.errors contains 'password' %} input--error{% endif %}"
        aria-required="true"
        {%- if form.errors contains 'password' -%}
          class="input--error"
          aria-invalid="true"
          aria-describedby="{{ formId }}-password-error"
        {%- endif -%}
      >
    
    	<h3>Spouse</h3>
     <input
        type="text"
        name="customer[tags][Spouse First Name]"
        placeholder="First Name"
        id="{{ formId }}-s-fname"
        aria-required="true"
            class="input input--round"
      >
    <input
        type="text"
         name="customer[tags][Spouse Last Name]"
        placeholder="Last Name"
        id="{{ formId }}-s-lname"
        aria-required="true"
           class="input input--round"
      >
    
     <input
        type="text"
         name="customer[tags][Spouse Email]"
        placeholder="Email"
        id="{{ formId }}-s-email"
        aria-required="true"
            class="input input--round"
      >
      <input
        type="submit"
        class="btn btn--primary btn--full"
        value="{{ 'customer.register.submit' | t }}"
      >

    {% endform %}
  </div>
</section>
